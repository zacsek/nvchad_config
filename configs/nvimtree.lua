local M = {
  -- git = {
  --   enable = true,
  -- },

  renderer = {
    -- highlight_git = true,

    icons = {
      -- show = {
      --   git = true,
      -- },

      glyphs = {
        folder = {
          default = "",
        },
      },
    },
  },
}

return M
